using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DictionaryNumber
{
    MonoBehaviour monobehaviour;
    public int number;
    public DictionaryNumber(MonoBehaviour mono,int num)
    {
        monobehaviour = mono;
        number = num;
    }
}
